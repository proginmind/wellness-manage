import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Unarchive Event Category",
};

export default function UnarchiveEventCategoryLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
