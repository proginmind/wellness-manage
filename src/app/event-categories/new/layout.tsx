import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Add Event Category",
};

export default function AddEventCategoryLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
