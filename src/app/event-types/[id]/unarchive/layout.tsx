import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Unarchive Event Type",
};

export default function UnarchiveEventTypeLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
