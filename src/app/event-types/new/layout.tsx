import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Add Event Type",
};

export default function AddEventTypeLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
